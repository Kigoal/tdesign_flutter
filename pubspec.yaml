name: tdesign_flutter
description: TDesign 适配移动端的组件库，适合在 flutter 技术栈项目中使用。
version: 0.0.1
homepage: 

environment:
  sdk: '>=3.1.5 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  image_picker: ^1.0.4
  provider: ^6.0.5
  tdesign_icons_flutter: ^1.0.0
  uuid: ^4.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
